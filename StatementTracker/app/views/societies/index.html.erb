<p id="notice"><%= notice %></p>

<h1>Societies</h1>

<%= render 'search_form', search_params: @search_params %>

<div id="nested-container">
  <% @societies.each do |society| %>
    <%= render 'node', element: society, scope: @search_params %>
  <% end %>
</div>

<!--table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Rut</th>
      <th>Parent</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @societies.each do |society| %>
      <tr>
        <td><%= society.name %></td>
        <td><%= society.rut %></td>
        <td><%= society.parent %></td>
        <td><%= link_to 'Show', society %></td>
        <td><%= link_to 'Edit', edit_society_path(society) %></td>
        <td><%= link_to 'Destroy', society, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table-->

<br>

<%= link_to 'New Society', new_society_path %>
