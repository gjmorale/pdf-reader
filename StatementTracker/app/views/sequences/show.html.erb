<h1>
  Recibidos al <%= @sequence.date.to_s %>
</h1>

<h2>
  <%= link_to "#{@sequence.tax.bank} - #{@sequence.tax.society}", @sequence.tax %> 
</h2>

<h2>Cartolas (<%= @sequence.statements.size %>/<%= @sequence.quantity %>):</h2>
<ul>
  <% @sequence.statements.each do |statement| %>
    <li><%= link_to statement.file_name, statement %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_sequence_path(@sequence) %> |
<%= link_to 'Back', sequences_path %>
